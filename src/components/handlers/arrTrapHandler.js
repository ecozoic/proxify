import { baseTrapHandler } from './trapHandler';

/**
 * Object that defines the traps used to proxy arrays.
 * Extends {@link handlers.BaseTrapHandler}.
 * @memberOf handlers
 */
var arrTrapHandler = Object.create(baseTrapHandler);

export { arrTrapHandler };
